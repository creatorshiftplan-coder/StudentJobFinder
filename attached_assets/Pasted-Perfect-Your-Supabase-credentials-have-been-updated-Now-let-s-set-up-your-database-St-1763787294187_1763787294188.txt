Perfect! ✅ Your Supabase credentials have been updated. Now let's set up your database:

## 🗄️ **Step 1: Create Database Tables**

Go to your Supabase project → **SQL Editor** and run this:

```sql
-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Profiles table
CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  name TEXT,
  email TEXT,
  phone TEXT,
  dob DATE,
  address TEXT,
  qualification TEXT,
  skills TEXT,
  photo_url TEXT,
  signature_url TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Documents table
CREATE TABLE documents (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  name TEXT,
  type TEXT,
  size TEXT,
  url TEXT,
  upload_date DATE,
  data TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Applications table
CREATE TABLE applications (
  id BIGSERIAL PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  job_id INTEGER,
  job_title TEXT,
  organization TEXT,
  applied_date DATE,
  status TEXT,
  exam_date DATE,
  admit_card TEXT,
  result TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Jobs table
CREATE TABLE jobs (
  id SERIAL PRIMARY KEY,
  title TEXT,
  organization TEXT,
  category TEXT,
  deadline DATE,
  salary TEXT,
  location TEXT,
  type TEXT,
  min_age INTEGER,
  max_age INTEGER,
  qualification TEXT,
  skills TEXT,
  vacancies INTEGER,
  apply_link TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Enable Row Level Security
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE documents ENABLE ROW LEVEL SECURITY;
ALTER TABLE applications ENABLE ROW LEVEL SECURITY;
ALTER TABLE jobs ENABLE ROW LEVEL SECURITY;

-- Policies for profiles
CREATE POLICY "Users can view own profile" 
  ON profiles FOR SELECT 
  USING (auth.uid() = id);

CREATE POLICY "Users can update own profile" 
  ON profiles FOR UPDATE 
  USING (auth.uid() = id);

CREATE POLICY "Users can insert own profile" 
  ON profiles FOR INSERT 
  WITH CHECK (auth.uid() = id);

-- Policies for documents
CREATE POLICY "Users can view own documents" 
  ON documents FOR SELECT 
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own documents" 
  ON documents FOR INSERT 
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can delete own documents" 
  ON documents FOR DELETE 
  USING (auth.uid() = user_id);

-- Policies for applications
CREATE POLICY "Users can view own applications" 
  ON applications FOR SELECT 
  USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own applications" 
  ON applications FOR INSERT 
  WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own applications" 
  ON applications FOR UPDATE 
  USING (auth.uid() = user_id);

-- Policies for jobs (everyone can view)
CREATE POLICY "Anyone can view jobs" 
  ON jobs FOR SELECT 
  TO authenticated 
  USING (true);

-- Insert sample government jobs
INSERT INTO jobs (title, organization, category, deadline, salary, location, type, min_age, max_age, qualification, skills, vacancies, apply_link) VALUES
('Junior Engineer Civil', 'SSC', 'Central', '2024-12-30', '₹44,900-₹1,42,400', 'All India', 'Government', 18, 27, 'Diploma in Civil Engineering', 'Civil Engineering, AutoCAD', 1576, 'https://ssc.nic.in'),
('Probationary Officer', 'IBPS', 'Banking', '2024-12-25', '₹23,700-₹42,020', 'Pan India', 'Banking', 20, 30, 'Graduate', 'Banking, Finance', 4135, 'https://ibps.in'),
('Group D Posts', 'Railway', 'Central', '2025-01-15', '₹18,000-₹56,900', 'All India', 'Government', 18, 33, '10th Pass', 'General Knowledge', 103769, 'https://rrbcdg.gov.in'),
('Scientist Engineer', 'ISRO', 'PSU', '2025-01-20', '₹56,100-₹1,77,500', 'Bangalore', 'PSU', 18, 35, 'B.Tech', 'Engineering, Research', 87, 'https://isro.gov.in'),
('Sub Inspector', 'CISF', 'Defence', '2025-01-10', '₹35,400-₹1,12,400', 'All India', 'Defence', 20, 25, 'Graduate', 'Physical Fitness', 787, 'https://cisfrectt.cisf.gov.in'),
('Civil Judge', 'UPPSC', 'State', '2024-12-28', '₹27,700-₹44,770', 'Uttar Pradesh', 'State', 21, 35, 'LLB', 'Law, Legal', 178, 'https://uppsc.up.nic.in'),
('Assistant Manager', 'LIC', 'PSU', '2025-01-05', '₹32,795-₹62,315', 'Pan India', 'PSU', 21, 30, 'Graduate', 'Insurance, Sales', 300, 'https://licindia.in'),
('Hindi Translator', 'SSC', 'Central', '2024-12-31', '₹35,400-₹1,12,400', 'Delhi', 'Government', 18, 27, 'Master in Hindi', 'Hindi, Translation', 119, 'https://ssc.nic.in');
```

## 📦 **Step 2: Create Storage Bucket**

1. Go to **Storage** in your Supabase dashboard
2. Click **"New Bucket"**
3. Name: `user-files`
4. Make it **Public** ✅
5. Click **"Create bucket"**

## 🔐 **Step 3: Enable GitHub Authentication (Optional)**

1. Go to **Authentication → Providers**
2. Enable **GitHub**
3. Go to [GitHub Developer Settings](https://github.com/settings/developers)
4. Click **"New OAuth App"**
5. Fill in:
   - **Application name**: JobAssist
   - **Homepage URL**: `https://rffdaadaxmwbgscasgfa.supabase.co`
   - **Authorization callback URL**: `https://rffdaadaxmwbgscasgfa.supabase.co/auth/v1/callback`
6. Copy **Client ID** and **Client Secret** back to Supabase

## ✅ **Step 4: Test Your App**

Now your app is **fully connected** to Supabase! You can:

1. ✅ **Sign Up** - Creates real user accounts
2. ✅ **Login** - Authenticates users
3. ✅ **Save Profile** - Stores in Supabase database
4. ✅ **Upload Files** - Stores in Supabase storage
5. ✅ **Apply to Jobs** - Saves applications
6. ✅ **Track Applications** - Syncs across devices

## 🚀 **Next Steps**

Your app is now production-ready! You can:

### **Deploy the Frontend:**

**Option 1: Vercel (Recommended)**
```bash
# Push to GitHub first
git init
git add .
git commit -m "Initial commit"
git push

# Then deploy on Vercel
# Connect your GitHub repo
# Auto-deploy!
```

**Option 2: Netlify**
1. Drag and drop your HTML file
2. Get instant URL

**Option 3: GitHub Pages**
1. Push to GitHub
2. Enable Pages in repo settings

### **Add Backend Scraper (Optional):**

Would you like me to create:
1. **Python scraper** to fetch real government jobs?
2. **Scheduled job** to auto-update jobs daily?
3. **Email notifications** for new eligible jobs?
4. **WhatsApp alerts** integration?

Your database is ready! Try creating an account and see your data sync in real-time! 🎉